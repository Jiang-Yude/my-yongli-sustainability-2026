# 操作日誌：部署機制升級與文件真相修正（2026-07-10）

> 依主庫 AGENTS.md「重大／結構性操作先寫計劃＋日誌」規則建立。
> 執行者：Claude（雷總管），計劃已經 Codex（gpt-5.6-sol）跨家主審抗辯後定案。

## 現狀快照（動工前）

- 分支：main，HEAD = `7bcecb4`，工作樹乾淨，與 origin/main 同步
- 線上：https://3481rctsi.vercel.app/ 中英首頁與本機 v7 雜湊一致
- 部署方式：本機從 `v7/` 手動 `vercel --prod --yes`（Vercel 專案 3481rctsi，team jiang-coach，projectId prj_AsFeRk75gQ05uaMojCLpSgpMyXWZ），未接 Git integration
- GitHub Pages：同 repo main 根目錄發布（v1 根 / v2 / v6 / v7 歷史備存）
- 全站內部連結 0 斷鏈；中英 profiles 各 15 頁對稱

## 要動什麼

1. 重寫 `v7/README.md` 與 `.claude/CLAUDE.md`（兩份仍停在 2026-05 v4 時代，會誤導操作者「從 v4/ 部署」）。歷史踩坑紀錄保留、標註為歷史。
2. 新增 `v7/_build/check.py` 品質閘門（斷鏈／中英對稱／build 冪等，失敗非零退出）＋ `.github/workflows/site-check.yml` CI。
3. Vercel 接 GitHub 自動部署：rootDirectory=v7、僅 v7 異動才部署。
4. 資料驅動化第一階段：members.html 中英社員卡抽成 `v7/_build/data/members.json`，build.py 以標記區塊渲染，輸出 diff 驗證與現版一致。

## 預期結果

- push main = 自動檢查＋自動部署，「線上與 GitHub 脫鉤」風險機制化消除
- 新增社員只改一份 JSON，中英列表自動同步

## 出錯怎麼還原

- 檔案層：一切經 git，`git revert` 或 `git reset --hard 7bcecb4` 後重新 `vercel --prod --yes` 即回動工前狀態
- Vercel 設定層：Git integration 可在專案設定 disconnect；rootDirectory 改回空值即回手動部署模式
- 線上內容驗證基準：中文首頁 md5 `38e57c4236eb088ecac2966c1f428e87`、英文首頁 md5 `4a88c6f8c1292969d67434ab206c168c`（若無內容改動，部署後應不變）

## 執行紀錄

- ✅ 文件真相修正：`v7/README.md` 全文重寫為 v7 現況；`.claude/CLAUDE.md` 改為「當前真相＋歷史教訓」結構；根 `README.md` 加當前真相導引（修正過期的 noindex 描述）
- ✅ 品質閘門：新增 `v7/_build/check.py`（現版全綠：1152 引用無斷鏈）＋ `.github/workflows/site-check.yml`（build 冪等已本機驗證）
- ✅ Vercel 設定（API）：rootDirectory=`v7`、commandForIgnoringBuildStep=`git diff --quiet HEAD^ HEAD -- .`（僅 v7 異動才部署）；`vercel git connect` 已連上 GitHub repo
- ✅ 自動部署首次實測（commit 20d9925）：push 後 Vercel 自動建置 READY 上線，GitHub Actions site-check 11 秒全綠，線上中英首頁雜湊與基準一致（內容零變動），全站抽查 6 種檔案類型皆 200（僅 README.md 由 Vercel 慣例不對外，無影響）
- ✅ 資料驅動化第一階段：中英 members.html 共 20 張社員卡抽成 `v7/_build/data/members.json`（單一來源），build.py 經 MEMBERS 標記自動渲染中英兩頁；遷移採 DOM 正規化比對驗證，新舊頁面語意完全一致（中英皆過），check.py 全綠
